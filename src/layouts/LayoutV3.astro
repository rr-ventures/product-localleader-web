---
export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en-AU" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <meta
      name="description"
      content="Local Leader version 3 scaffold for a future landing page build focused on Australian tradies."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Lora:ital,wght@0,500;0,600;1,500&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
  </head>
  <body class="bg-[#faf8f4] text-slate-900 antialiased selection:bg-slate-900 selection:text-white">
    <slot />
  </body>
</html>

<style is:global>
  :root {
    --font-sans: "DM Sans", system-ui, sans-serif;
    --font-serif: "Lora", Georgia, serif;
  }

  html {
    font-family: var(--font-sans);
  }

  .font-serif {
    font-family: var(--font-serif);
  }
</style>
